# postpilot

PostPilot is a lightweight Flask-based application designed to automate social media posts from JSON files. It supports Instagram, tour guides, and visa updates via simple endpoints and can be run locally or deployed using Docker.